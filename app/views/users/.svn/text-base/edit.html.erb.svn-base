<h1>Editing user</h1>
<%= form_for :user, url: user_path(@user.id), method: :patch do |u| %>
	<% if @user.errors.any? %>
		<div id="errorExplanation">
			<h2><%= pluralize(@user.errors.count, "error")%> prohibited
      this post from being saved:</h2>
      		<ul>
      			<% @user.errors.full_messages.each do |msg| %>
      				<li><%= msg%></li>
      			<% end %>
      		</ul>
		</div>
	<% end %>
	<p>
		<%= u.label :name%>
		<%= u.text_field :name%>
	</p>
	<p>
		<%= u.label :email%>
		<%= u.text_field :email%>
	</p>
	<p>
		<%= u.submit %>
	</p>
<% end %>

<%= link_to 'Back', users_path%>
